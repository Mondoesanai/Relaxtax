Ambient entry track. Swap ambient.mp3 with any looping tropical/chill track (keep the filename).
Current: 'Sunset Island Voyage' by Djovan.
Played from every page via <audio id="ambient-audio">; starts on the intro's 'Step onto the island' button and loops.
serve.mjs supports HTTP Range so it streams fast locally; real hosts (Vercel/Netlify) do this automatically.
If ambient.mp3 is missing, the site falls back to a synthesized ocean loop.
TIP: a smaller file (~96-128kbps) starts even faster — re-encode when you get a chance.
